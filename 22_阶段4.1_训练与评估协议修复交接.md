# Stage 4.1 训练与评估协议修复交接

## 1. 范围、源文档冲突与冻结边界

本次仅修复实验协议、RNG/seed 管理、旧周期 checkpoint 重评、结果落盘、训练诊断与 resume 元数据。未修改 Environment v1.1、通信/天线、Reward、outage、safety、26 维 local observation、47 维 Critic 输入、P0–P4 Actor 结构、PPO/GAE 数学、old latent `z` 复用或 safety-priority key。

任务书要求预读的 `20_Stage4_Role收口与Graph启动交接.md` 与 `00_论文当前总体方案与决策记录_阶段3收口版.md` 在本工作区不存在。实际可用历史交接位于 `过往开发文档/`；本次以当前源码、实际 checkpoint 和 artifacts 为准，并在 `protocol_audit.json` 中显式记录该缺失，未以旧文档文字替代可复核产物。

## 2. 修改文件

- `plain_mappo/experiment_protocol.py`：Stage 4.1 protocol version、固定 validation/test seeds、5 个将来 paired-run seed reservation、稳定 seed 派生、manifest/hash/provenance helper。
- `plain_mappo/config.py`：序列化 `protocol_version`、run/RNG domain seeds、manifest 路径与 resume 语义；历史 checkpoint 缺字段时明确标为 `legacy_protocol`。
- `plain_mappo/networks.py`：Actor sampling 可接收独立 action-noise generator，Normal 采样分布与 PPO latent-z 数学不变。
- `plain_mappo/trainer.py`：Actor/Critic 初始化、action noise、minibatch permutation 使用独立可保存 RNG stream；增加 value/gradient diagnostics；checkpoint 写入 protocol/RNG/resume metadata。
- `plain_mappo/metrics.py`：只读 evaluation diagnostics 增加 `first_outage_step` 与 `terminal_step`。
- `train_plain_mappo.py`：增加 `--run-seed`，为将来正式 paired run 重新派生全部独立 RNG domain。
- `reevaluate_checkpoint_grid.py`：只读数值排序 checkpoint-grid evaluator，保存 raw Episode JSONL；test split 仅允许一个已选择 checkpoint。
- `stage4_1_protocol_validation.py`：prepare → validation-only select → final-test audit 的顺序化协议脚本。
- `selfcheck/test_mappo_protocol.py`：新增 Stage 4.1 测试。

## 3. 协议版本与 seed manifests

协议：`stage4.1-v1`。

- Validation seeds：`50,000,000..50,000,019`（20 Episodes；允许 checkpoint selection）。
- Final-test seeds：`60,000,000..60,000,049`（50 Episodes；禁止 selection）。
- Manifest：`artifacts/stage4-1-protocol-repair/seed_manifest.json`
- Manifest SHA-256：`d2622a91c144aa7d32ccd5bbf0353694ba281a2044b7985a0290d4c9e7227acb`

未来正式 paired run 的 training-environment reservations 已冻结：run seeds 2026–2030 分别对应 `[10M,12M)`、`[12M,14M)`、`[14M,16M)`、`[16M,18M)`、`[18M,20M)`。每一 run 的 actor init、critic init、action noise、minibatch permutation 和 env seed base 都由 `run_seed` 稳定派生并写入 config/checkpoint。相同 run seed 下，Actor variant 不再影响 Critic 初始化、action-noise 或 minibatch RNG state。

## 4. 自动测试

真实执行：

```text
python -m unittest discover -s selfcheck -p "test_*.py" -v
```

最终结果：**66/66 PASS**。

新增协议测试覆盖：validation/test/reserved training seeds 分离、同 run seed 跨 Actor variant 的 Critic/RNG 公平性、独立 RNG checkpoint round-trip、legacy checkpoint 显式标记、checkpoint 数值排序、test 禁止 grid selection、同分较早 update、Episode 行数等于 checkpoint×seed 且无重复、explained variance/gradient clipping 手算一致、新日志诊断有限或显式空值。

## 5. 10-update diagnostics smoke

真实运行：`artifacts/stage4-1-protocol-repair/diagnostics-smoke/`。

- P0 frozen configuration、10 updates、10,240 team-time samples。
- 无 NaN/Inf；`finite=1`。
- 最后 update：KL=0.003697，clip fraction=0.041040，Actor/Critic mean pre-clip norm=0.521110/101.269548。
- Actor/Critic max pre-clip norm=0.829323/162.873734；gradient clip fraction=0.60/1.00。
- value target mean/std=24.978790/12.849008；prediction pre mean/std=14.383151/6.567277；explained variance=0.467212。
- `actor_final.pt` 真实重载后 fixed 5-seed deterministic evaluation 与 `eval.csv` 严格一致。

这是诊断 smoke，不构成策略性能或方法有效性证据。

## 6. P0/P1/P2 validation-grid（仅 validation split）

三个历史 single-seed runs 的 20 个周期 Actor checkpoint 均使用同一 20-seed validation set 重评，共 60 checkpoint / 1,200 raw Episode records。完整表和每条 Episode 分别位于：

- `artifacts/stage4-1-protocol-repair/validation_grid.csv`
- `artifacts/stage4-1-protocol-repair/validation_episodes.jsonl`

按既有 completion-first safety-priority key、且完全同分选择更早 update，得到：

| Label | Variant | selected update | validation normal / 20 | persistent outage | collision | boundary | mean e2e Mbps | outage ratio | rate satisfaction |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| P0 | plain | 1000 | 0 | 20 | 0 | 0 | 6.298344 | 0.123267 | 0.816372 |
| P1 | role_info | 800 | 0 | 9 | 0 | 11 | 6.630985 | 0.102127 | 0.878465 |
| P2 | role_head | 650 | 0 | 15 | 0 | 5 | 6.222161 | 0.145216 | 0.825264 |

`selected_checkpoints.json` 在 final test 前已写入，包含 update、path、SHA-256、validation score、selection rule、manifest hash 与选择时间。所有历史 checkpoint 的 config 在新加载器中明确标为 `legacy_protocol`，并未被伪装为使用了新的独立 RNG protocol。

执行 trace 位于 `artifacts/stage4-1-protocol-repair/evaluation_invocations.jsonl`。初次 P0 全-grid 调用在宿主约 30 秒单次限制处结束，但已写入的结果可按 hash/seed 完整性复核；补齐时产生的 P0 update 950/1000 重复 summary/raw rows 已仅在本轮新建 Stage 4.1 输出中按 `(checkpoint_sha256, seed)` 机械去重。最终 validation 行严格为 60×20=1,200，不影响任一历史训练 artifact。

## 7. 独立 50-episode final test

三个 final test 均只使用已锁定 checkpoint，统一 seeds `60,000,000..60,000,049`；test 结果未用于重新选择 checkpoint。

| Variant | selected update | normal / 50 | persistent outage | collision | boundary | hard violations | mean e2e Mbps | outage ratio | rate satisfaction |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| plain | 1000 | 0 | 44 | 1 | 5 | 0 | 6.338344 | 0.112996 | 0.817126 |
| role_info | 800 | 0 | 14 | 0 | 36 | 0 | 6.847062 | 0.070595 | 0.915737 |
| role_head | 650 | 0 | 34 | 0 | 16 | 0 | 6.235161 | 0.133500 | 0.825030 |

实际输出：

- `artifacts/stage4-1-protocol-repair/final_test_summary.csv`
- `artifacts/stage4-1-protocol-repair/final_test_episodes.jsonl`

Episode 原始字段复用 `EpisodeMetrics` 实际名称，空间最小距离字段为 `min_separation_m`（任务书预期的 `min_node_distance_m` 的实际实现名）；另含 return、length、termination、first outage step、terminal step、速率、outage、速度/加速度、action saturation 与 movement distance。final raw 行数=150，且与 selected checkpoint hash×50 seeds 可反算一致。

这些仅是 P0/P1/P2 各一个历史训练 seed 的 retrospective pilot 结果；不支持 Actor 结构的稳定排名或有效性结论。

## 8. Critic / grad 诊断结论

历史 run 的 Critic pre-clip gradient norm（min / mean / max）为：P0=14.335824/50.130775/159.758854；P1=8.742287/31.312344/123.774209；P2=12.762234/34.609306/194.510475。它们都远高于固定 `max_grad_norm=0.5`，而历史 CSV 没有 value target / prediction / explained-variance 字段，不能事后虚构 Critic 拟合结论。

新增 smoke 能观测 explained variance 和 clip fraction，但也显示 Critic clipping=1.0。结论：`PRE-EXPERIMENT REQUIRED`。这只表示在未来正式 multi-seed 实验前，需要一个新的、对称且独立授权的 value-treatment 前置实验决策；本轮没有启用 value normalization、value clipping 或修改 critic LR。

## 9. Gamma / horizon 诊断结论

固定 `dt=0.2 s`、episode horizon=500 steps=100 s、`lambda=0.95`：

| gamma | step half-life | seconds half-life | terminal weight at 100 s | GAE trace half-life seconds |
|---:|---:|---:|---:|---:|
| 0.990 | 68.968 | 13.794 | 0.00657048 | 2.260 |
| 0.995 | 138.283 | 27.657 | 0.08157186 | 2.462 |
| 0.997 | 230.702 | 46.140 | 0.22262768 | 2.553 |
| 0.999 | 692.801 | 138.560 | 0.60637894 | 2.651 |

validation-selected checkpoint 的终止/first-outage 时间来自完整 raw Episode 记录：P0 length mean=213.6，first outage mean=189.6；P1 length mean=113.85，first outage mean=93.0，boundary termination 11/20；P2 length mean=146.75，first outage mean=123.22，boundary termination 5/20。

结论：`PRE-EXPERIMENT REQUIRED`。gamma=0.99 的 100-s terminal direct weight 仅 0.00657，且 selected validation failures 多发生在 65–313 steps；这是明确的长时域 credit-assignment 风险，但不是本轮修改 gamma 的授权或 gamma sweep 的结果。

## 10. Resume 语义确认

新 config 和 checkpoint metadata 明确写入：

```text
resume_mode = fresh_episode_at_update_boundary
exact_environment_resume = false
```

恢复包含 Actor/Critic、optimizers、normalizer、全局 RNG、独立 action/minibatch RNG 与 never-used env seed progress；不保存环境内部状态、`current_obs`、半段 rollout 或当前 episode return/length。因此恢复会在 PPO update boundary 以 fresh episodes 继续，不能表述为与 uninterrupted run bitwise-exact 相同。历史 checkpoint 可加载，但明确标记 `legacy_protocol`。

## 11. 将来正式训练命令矩阵（仅计划，未执行）

Stage 4.1 PASS 后的后续训练仍需新的明确授权。届时 P0–P4 每个 variant 至少 3、目标 5 个 paired run seeds，均使用同一 protocol manifest 和 1000-update budget。例如：

```text
python train_plain_mappo.py --full --actor-variant plain         --run-seed 2026 --output-dir artifacts/formal/P0/run_2026
python train_plain_mappo.py --full --actor-variant role_info     --run-seed 2026 --output-dir artifacts/formal/P1/run_2026
python train_plain_mappo.py --full --actor-variant role_head     --run-seed 2026 --output-dir artifacts/formal/P2/run_2026
python train_plain_mappo.py --full --actor-variant topology_info --run-seed 2026 --output-dir artifacts/formal/P3/run_2026
python train_plain_mappo.py --full --actor-variant graph         --run-seed 2026 --output-dir artifacts/formal/P4/run_2026
```

对 run seeds 2027–2030 重复；每次 run 先冻结周期 checkpoints，再用 validation manifest 选择，再运行 final test。统计单位是独立 training run，不能把 evaluation Episodes 视为独立训练重复。

## 12. 未执行内容与最终状态

未执行：P0/P1/P2 新 1000-update、P3/P4 正式 1000-update、P5/P6、Role+Graph、任何 gamma/lambda/value/critic-LR/Actor/Reward/Environment 调参或修改。

Stage 4.1 protocol repair = COMPLETE

Engineering validation = PASS

P0/P1/P2 retrospective status = SINGLE-SEED PILOT

P3 formal training = NOT STARTED

P4 formal training = NOT STARTED

Multi-seed P0-P4 training = NOT AUTHORIZED

Role+Graph = NOT STARTED
